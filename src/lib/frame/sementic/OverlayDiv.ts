import styled from 'styled-components'
import { FlexCenterDiv } from '../generic'

export const OverlayDiv = styled(FlexCenterDiv)`
  position: absolute;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
`
